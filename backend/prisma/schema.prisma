generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

<<<<<<< HEAD
model Barbershop {
  id        Int       @id @default(autoincrement())
  name      String
  address   String
  img       String 
  phone     String
  latitude  Float
  longitude Float
  createdAt DateTime? @default(now())
  updatedAt DateTime? @updatedAt()
=======
enum Role {
  ADMIN
  USER
  BARBER
}

model Barbershop {
  id           Int           @id @default(autoincrement())
  name         String
  address      String
  img          String
  phone        String
  latitude     Float
  longitude    Float
  createdAt    DateTime?     @default(now())
  updatedAt    DateTime?     @updatedAt()
  appointments Appointment[]
}

model User {
  id           Int           @id @default(autoincrement())
  name         String
  email        String        @unique
  password     String
  role         Role
  createdAt    DateTime?     @default(now())
  updatedAt    DateTime?     @updatedAt()
  appointments Appointment[]
}

model Appointment {
  id       Int        @id @default(autoincrement())
  date     DateTime   @default(now())
  client   User       @relation(fields: [userId], references: [id])
  userId   Int
  barber   Barbershop @relation(fields: [barberId], references: [id])
  barberId Int
>>>>>>> 8b9bafa756a6e939659882d7b26a74d247360ec9
}
